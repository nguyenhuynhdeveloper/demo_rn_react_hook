import { Text, StyleSheet, View, SafeAreaView } from 'react-native'
import React, { Component, useCallback, useContext, useEffect , useState, useReducer, useRef, useMemo } from 'react'

// Lưu ý : Hook phải được viết và khai báo và viết trong function 

const Hook_useState = () => {
// const [render , setRender] = useState(false)
    return (
      <SafeAreaView>
        <Text>Hook_useState</Text>
      </SafeAreaView>
    )
  }

export default Hook_useState

const styles = StyleSheet.create({})